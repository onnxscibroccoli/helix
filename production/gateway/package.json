[Reading 20 lines from start (total: 20 lines, 0 remaining)]

{
  "name": "gateway",
  "version": "1.0.0",
  "description": "The gateway is the only public desktop entry point. It terminates OIDC login, creates an HttpOnly Secure session cookie, authorizes a workspace, issues a short-lived signed desktop WebSocket ticket, and proxies that WebSocket to the workspace localhost VNC socket.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/client": "^2.1.0",
    "@modelcontextprotocol/server": "^2.1.0",
    "ioredis": "^5.11.1",
    "pg": "^8.23.0",
    "zod": "^4.6.5"
  }
}

[executed on device: ip-172-31-8-59 (882f1036-235b-4669-acaf-1e1135b156bd)]